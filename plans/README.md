# Implementation Plans

Generated by the improve skill on 2026-06-25. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Establish Backend Verification Baseline | P1 | M | - | DONE |
| 002 | Block Public Actions On Completed Appointments | P1 | S | 001 | DONE |
| 003 | Characterize And Fix Appointment Conflict Concurrency | P2 | M | 001 | BLOCKED - requires explicit disposable MySQL test database |
| 004 | Add Pagination And Period Filters To Appointments | P3 | M | 001 | TODO |

## Dependency notes

- 002, 003, and 004 depend on 001 because behavior changes should land with backend tests instead of relying only on manual checks.
- 003 should run before any future advanced availability feature because it protects the core no-conflict invariant.
- 004 can wait until after the TCC demo if data volume is still small; it is a scalability plan, not an immediate correctness blocker.

## Findings considered and rejected

- Committed `.env` secret exposure: rejected because `.env` exists locally but is ignored by `.gitignore` and `git ls-files` did not show it as tracked. Do not reproduce or inspect local secret values.
- High/critical dependency advisories: rejected because `npm audit --audit-level=high --omit=dev` returned 0 vulnerabilities for backend and frontend during recon.
- Upload file type validation missing: rejected because `backend/src/middlewares/uploadNegocioMiddleware.js` filters declared image MIME types and `backend/src/utils/imageStorage.js` validates binary signatures before writing.

## Verification commands observed during recon

- Backend dev: `npm.cmd run dev` from `backend/`
- Backend production start: `npm.cmd start` from `backend/`
- Backend audit: `npm.cmd audit --audit-level=high --omit=dev` from `backend/`
- Frontend dev: `npm.cmd run dev` from `frontend/`
- Frontend build: `npm.cmd run build` from `frontend/`
- Frontend audit: `npm.cmd audit --audit-level=high --omit=dev` from `frontend/`
- Git whitespace check: `git diff --check` from repo root

There was no backend `test` script at planning time; Plan 001 establishes it.
